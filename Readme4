var item = this.__getItemByIndex(parseInt(selectedIndex));
		if(item && item[this.__fieldItem])
		{
			this.__scrollTarget.scrollTop = (item[this.__fieldItem].offsetTop);;
		}
		this.__divOuterContainer.style.overflowY="hidden";
