var setting = {element: this.__divCenterTableBodyContainer,enableCustomHandler: true,verticalScrollButtons:{enable:true},horizontalScrollButtons:{enable:true}};
			for (var key in setting) 
			{ 
				this.__scrollerSetting[key] = setting[key]; 
			}
      
      <style>
  	.content{
  		margin: 0;
    	padding: 10px;
    	min-width: 300px;
  	}
  	.moveAble {
  	display:none;
    position: absolute;
    top:0px;
    right:10px;
    z-index: 1000;
}

.info {
  background-color:red;
  height: 100px;
  width:100px;
}

<p class="content">Content in Scrollbar</p>
<div style="width:1000px;"><h3>End of Vertical and horizontal scrollbar and this demo is great.End of Vertical and horizontal scrollbar and this demo is great.End of Vertical and horizontal scrollbar and this demo is great.End of Vertical and horizontal scrollbar and this demo is great</h3></div>

var element = document.querySelector('#divBody');
			var setting = {element:element,enableCustomHandler:true,verticalScrollButtons:{enable:true},horizontalScrollButtons:{enable:true},
							scrollHeight: element.scrollHeight,scrollWidth: element.scrollWidth};
			var scroller2 = new NSScroller(setting);
			element.addEventListener(NSScroller.SCROLLING,customScrollHandler);
