var visibleIndex = item[this.__fieldVisibleIndex];
		//to get actual rowIndex use row.rowIndex
		this.__objGrid.__resetDataInBody(visibleIndex,visibleIndex + rowLength,row.rowIndex);
		
element = this.util.getElement(element);
