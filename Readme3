Line 77: this.__contextMenuSetting = null;
Line 288: nsGrid.setContextMenuSetting = function(contextMenuSetting)
{
	this.__contextMenuSetting = contextMenuSetting;
};
Line 784: 		var divHeaderContainer = this.getElement(this.getID() + this.__TABLE_HEADER_CONTAINER_ID);
		if(divHeaderContainer)
		{
			divHeaderContainer.parentNode.removeChild(divHeaderContainer);
		}
980: nsGrid.prototype.__removeHeader = function()
{
	if(this.__tblHeader && this.__tblHeader.tHead)
	{
		this.__tblHeader.deleteTHead();
	}
	if(this.__tblHeader.tBodies && this.__tblHeader.tBodies.length > 0)
    {
    	this.__tblHeader.removeChild(this.__tblHeader.tBodies[0]);
    }
};

1754: remove nsGrid.__lastYPosition = 0;

Line 2470: if(this.__contextMenuSetting)
		{
			for(var key in this.__contextMenuSetting)
			{
				//grid setting should not be overriden by the users
				if(!contextMenuSetting[key])
				{
					contextMenuSetting[key] = this.__contextMenuSetting[key];
				}
			}
		}
		

nsmenu:
125: width: parseInt(this.__setting["width"]) || -1
152: if(this.__config.width > -1)
	{
		parentNode.style.width = this.__config.width + "px";
	}
